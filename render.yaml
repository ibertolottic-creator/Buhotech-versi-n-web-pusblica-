services:
  - type: web
    name: buhotech-labs
    env: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: GEMINI_API_KEYS
        sync: false
      - key: APP_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        value: sqlite+aiosqlite:///./buhotech.db
